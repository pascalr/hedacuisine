<h1>Nouveau livre de recette</h1>

<%= form_with(model: @book, local: true) do |form| %>
  <% if @book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
        <% @book.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, size: 50 %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<h2>Choisir un thème</h2>
<% book = Book.new %>
<% book.user = current_user %>
<div style="display: flex; gap: 20px;">
  <% book.name = "Theme light" %>
  <%= render partial: "books/title_page_thumbnail", locals: {book: book} %>
  <% book.name = "Theme dark" %>
  <%= render partial: "books/title_page_thumbnail", locals: {book: book} %>
  <% book.name = "Theme sky" %>
  <%= render partial: "books/title_page_thumbnail", locals: {book: book} %>
</div>

<p>TODO: Choisir le thème</p>
