<%= link_to "Nouveau livre", new_book_path %> |
<%= link_to "Trouver un livre", new_book_path %>

<h1>Livres</h1>

<div style="display: flex; gap: 20px; flex-wrap: wrap;">
  <% @books.each do |book| %>
    <%= link_to book do %>
      <%= render partial: "books/title_page_thumbnail_fixed_height", locals: {book: book} %>
    <% end %>
  <% end %>
</div>
