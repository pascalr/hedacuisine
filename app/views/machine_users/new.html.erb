<%= form_with(model: @machine_user, local: true) do |form| %>
  <% if @machine_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@machine_user.errors.count, "error") %> prohibited this machine user from being saved:</h2>

      <ul>
        <% @machine_user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nickname %>
    <%= form.text_field :nickname %>
  </div>
  
  <div class="field">
    <%= form.label :machine_id %>
    <%= form.collection_select :machine_id, Machine.all, :id, :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
