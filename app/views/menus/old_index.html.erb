<%= link_to 'Nouveau menu', new_menu_path if current_user %>

<div class="responsive-margins-sm">
  <% if current_user %>
    <h2 style="font-size: 2em;">Mes menus 🧑‍🍳</h2>
    <hr>
    <%= render partial: current_user.menus %>
    <% unless true and current_user.favorite_menus.blank? %>
      <h2 style="font-size: 2em;">Mes favoris ❤️</h2>
      <hr>
      <%= render partial: current_user.favorite_menus %>
    <% end %>
  <% else %>
  
    <h2 style="font-size: 2em;">Menus Heda 🦾</h2>
    <hr>
    <% user = User.first # FIXME: Temporary %>
    <%= render partial: user.menus %>
  <% end %>
</div>

<%# render partial: @menus %>

<%# javascript_pack_tag "recipe_slider_pack" %>


Ca ca allait dans menus/_menu.html.erb

<span class="tagname">
  <%= menu.is_cookable ? image_tag("logo.png", size: "24x24") : "" %>
  <%= link_to menu.name, menu, class: 'not-link' %>
</span>
<%# render file: "/menus/_recipe_list", locals: {tag_id: menu.id, recipes: menu.recipes.select(&:image)} %><br>
